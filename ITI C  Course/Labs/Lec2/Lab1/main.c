/*
  Standard Libraries
*/

#include <stdio.h> // input/output std library
#include <stdbool.h> // standard library for boolean data type

//main function -> our own entry point
void main (void){

  //declare and initialize booth T, F variables
  bool T = true;
  bool F = false;
  
  // Print the value for both T & F variables
  printf("False Value in GCC = %d\n", F);
  printf("True Value in GCC = %d\n", T);

}
